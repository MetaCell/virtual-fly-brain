const initialState = {
  graphQueryIndex : -1,
  sync : false,
  instanceOnFocus : undefined
};

const GraphReducer = (state = initialState, action) => {
  return state;
}
export default GraphReducer;