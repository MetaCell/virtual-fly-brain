openapi: 3.0.2
info:
  title: VirtualFlyBrainIndex
  version: 1.0.0
servers:
- url: /
tags:
- name: molecule
paths:
  /ingestion:
    get:
      operationId: ingest
      responses:
        "200":
          content:
            application/json:
              schema:
                type: string
          description: ok
      x-openapi-router-controller: virtual_fly_brain.controllers.default_controller
  /molecules/component:
    description: The REST endpoint/path used to list and create zero or more `Molecule`
      entities.  This path contains a `GET` and `POST` operation to perform the list
      and create tasks, respectively.
    get:
      description: Gets a list of all `Molecule` entities.
      operationId: get_component
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns an array of `Molecule` entities.
      summary: List All Molecules
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage the list of molecules.
  /molecules/component/{term}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_component
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/component/{term}/properties/{tables}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_component_properties
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: List of tables to search.
        explode: false
        in: path
        name: tables
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
        "400":
          description: Parameter must be integer
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/inchi:
    description: The REST endpoint/path used to list and create zero or more `Molecule`
      entities.  This path contains a `GET` and `POST` operation to perform the list
      and create tasks, respectively.
    get:
      description: Gets a list of all `Molecule` entities.
      operationId: get_inchi
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule_Inchi'
                type: array
          description: Successful response - returns an array of `Molecule` entities.
      summary: List All Molecules
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage the list of molecules.
  /molecules/inchi/{term}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_inchi
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule_Inchi'
                type: array
          description: Successful response - returns a single `Molecule`.
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/inchi/{term}/properties/{tables}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_inchi_properties
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: List of tables to search.
        explode: false
        in: path
        name: tables
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
        "400":
          description: Parameter must be integer
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/iupac:
    description: The REST endpoint/path used to list and create zero or more `Molecule`
      entities.  This path contains a `GET` and `POST` operation to perform the list
      and create tasks, respectively.
    get:
      description: Gets a list of all `Molecule` entities.
      operationId: get_iupac
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns an array of `Molecule` entities.
      summary: List All Molecules
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage the list of molecules.
  /molecules/iupac/{term}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_iupac
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/iupac/{term}/properties/{tables}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_iupac_properties
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: List of tables to search.
        explode: false
        in: path
        name: tables
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
        "400":
          description: Parameter must be integer
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/mass:
    description: The REST endpoint/path used to list and create zero or more `Molecule`
      entities.  This path contains a `GET` and `POST` operation to perform the list
      and create tasks, respectively.
    get:
      description: Gets a list of all `Molecule` entities.
      operationId: get_mass
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns an array of `Molecule` entities.
      summary: List All Molecules
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage the list of molecules.
  /molecules/mass/{term}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_mass
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/mass/{term}/properties/{tables}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_mass_properties
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: List of tables to search.
        explode: false
        in: path
        name: tables
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
        "400":
          description: Parameter must be integer
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/mesh:
    description: The REST endpoint/path used to list and create zero or more `Molecule`
      entities.  This path contains a `GET` and `POST` operation to perform the list
      and create tasks, respectively.
    get:
      description: Gets a list of all `Molecule` entities.
      operationId: get_mesh
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns an array of `Molecule` entities.
      summary: List All Molecules
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage the list of molecules.
  /molecules/mesh/{term}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_mesh
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/mesh/{term}/properties/{tables}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_mesh_properties
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: List of tables to search.
        explode: false
        in: path
        name: tables
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
        "400":
          description: Parameter must be integer
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/parent:
    description: The REST endpoint/path used to list and create zero or more `Molecule`
      entities.  This path contains a `GET` and `POST` operation to perform the list
      and create tasks, respectively.
    get:
      description: Gets a list of all `Molecule` entities.
      operationId: get_parent
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns an array of `Molecule` entities.
      summary: List All Molecules
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage the list of molecules.
  /molecules/parent/{term}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_parent
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/parent/{term}/properties/{tables}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_parent_properties
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: List of tables to search.
        explode: false
        in: path
        name: tables
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
        "400":
          description: Parameter must be integer
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/patent:
    description: The REST endpoint/path used to list and create zero or more `Molecule`
      entities.  This path contains a `GET` and `POST` operation to perform the list
      and create tasks, respectively.
    get:
      description: Gets a list of all `Molecule` entities.
      operationId: get_patent
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns an array of `Molecule` entities.
      summary: List All Molecules
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage the list of molecules.
  /molecules/patent/{term}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_patent
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/patent/{term}/properties/{tables}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_patent_properties
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: List of tables to search.
        explode: false
        in: path
        name: tables
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
        "400":
          description: Parameter must be integer
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/pmid:
    description: The REST endpoint/path used to list and create zero or more `Molecule`
      entities.  This path contains a `GET` and `POST` operation to perform the list
      and create tasks, respectively.
    get:
      description: Gets a list of all `Molecule` entities.
      operationId: get_pmid
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns an array of `Molecule` entities.
      summary: List All Molecules
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage the list of molecules.
  /molecules/pmid/{term}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_pmid
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/pmid/{term}/properties/{tables}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_pmid_properties
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: List of tables to search.
        explode: false
        in: path
        name: tables
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
        "400":
          description: Parameter must be integer
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/sid:
    description: The REST endpoint/path used to list and create zero or more `Molecule`
      entities.  This path contains a `GET` and `POST` operation to perform the list
      and create tasks, respectively.
    get:
      description: Gets a list of all `Molecule` entities.
      operationId: get_sid
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns an array of `Molecule` entities.
      summary: List All Molecules
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage the list of molecules.
  /molecules/sid/{term}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_sid
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/sid/{term}/properties/{tables}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_sid_properties
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: List of tables to search.
        explode: false
        in: path
        name: tables
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
        "400":
          description: Parameter must be integer
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/smiles:
    description: The REST endpoint/path used to list and create zero or more `Molecule`
      entities.  This path contains a `GET` and `POST` operation to perform the list
      and create tasks, respectively.
    get:
      description: Gets a list of all `Molecule` entities.
      operationId: get_smiles
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns an array of `Molecule` entities.
      summary: List All Smiles
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage the list of molecules.
  /molecules/smiles/{term}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_smiles
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/smiles/{term}/properties/{tables}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_smiles_properties
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: List of tables to search.
        explode: false
        in: path
        name: tables
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
        "400":
          description: Parameter must be integer
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/synonyms:
    description: The REST endpoint/path used to list and create zero or more `Molecule`
      entities.  This path contains a `GET` and `POST` operation to perform the list
      and create tasks, respectively.
    get:
      description: Gets a list of all `Molecule` entities.
      operationId: get_synonyms
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns an array of `Molecule` entities.
      summary: List All Molecules
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage the list of molecules.
  /molecules/synonyms/{term}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_synonyms
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/synonyms/{term}/properties/{tables}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_synonyms_properties
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: List of tables to search.
        explode: false
        in: path
        name: tables
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
        "400":
          description: Parameter must be integer
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/synonyms_unfiltered:
    description: The REST endpoint/path used to list and create zero or more `Molecule`
      entities.  This path contains a `GET` and `POST` operation to perform the list
      and create tasks, respectively.
    get:
      description: Gets a list of all `Molecule` entities.
      operationId: get_synonyms_unfiltered
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns an array of `Molecule` entities.
      summary: List All Molecules
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage the list of molecules.
  /molecules/synonyms_unfiltered/{term}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_synonyms_unfiltered
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/synonyms_unfiltered/{term}/properties/{tables}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_synonyms_unfiltered_properties
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: List of tables to search.
        explode: false
        in: path
        name: tables
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
        "400":
          description: Parameter must be integer
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/title:
    description: The REST endpoint/path used to list and create zero or more `Molecule`
      entities.  This path contains a `GET` and `POST` operation to perform the list
      and create tasks, respectively.
    get:
      description: Gets a list of all `Molecule` entities.
      operationId: get_title
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns an array of `Molecule` entities.
      summary: List All Molecules
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage the list of molecules.
  /molecules/title/{term}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_title
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
  /molecules/title/{term}/properties/{tables}:
    description: The REST endpoint/path used to get single instances of an `Molecule`.  This
      path contains `GET` operations used to perform the get tasks, respectively.
    get:
      description: Gets the details of a single instance of a `Molecule`.
      operationId: search_title_properties
      parameters:
      - description: A unique identifier for a `Molecule`.
        explode: false
        in: path
        name: term
        required: true
        schema:
          type: string
        style: simple
      - description: List of tables to search.
        explode: false
        in: path
        name: tables
        required: true
        schema:
          type: string
        style: simple
      - description: Return results matching exact query
        explode: true
        in: query
        name: exactMatch
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Molecule'
                type: array
          description: Successful response - returns a single `Molecule`.
        "400":
          description: Parameter must be integer
      summary: Get a Molecule
      tags:
      - molecule
      x-openapi-router-controller: virtual_fly_brain.controllers.molecule_controller
    summary: Path used to manage a single Molecule search by CID.
components:
  schemas:
    Molecule:
      description: List of synonyms matching search query
      example:
        synonym: dimethyl
        cid: 1
      properties:
        cid:
          example: 1
          title: cid
          type: integer
        synonym:
          description: List of synonyms matching search query
          example: dimethyl
          title: synonym
          type: string
        exact_match:
          description: Flag true if it matches search exactly
          example: true
          title: match
          type: boolean
      title: Molecule
      type: object
    Molecule_Inchi:
      description: List of synonyms matching search query
      example:
        inchi: dimethyl
        key: 1
        cid: 1
      properties:
        cid:
          example: 1
          title: cid
          type: integer
        inchi:
          description: List of synonyms matching search query
          example: dimethyl
          title: inchi
          type: string
        key:
          description: List of synonyms matching search query
          example: dimethyl
          title: key
          type: string
        exact_match:
          description: Flag true if it matches search exactly
          example: true
          title: match
          type: boolean
      title: Molecule_Inchi
      type: object
