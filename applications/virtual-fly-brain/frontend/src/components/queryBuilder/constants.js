import vars from "../../theme/variables";

const { listHeadingColor } = vars;

export const dividerStyle = {
  height: '0.875rem', 
  width: '0.0625rem', 
  background: listHeadingColor, 
  borderRadius: '0.125rem'
};
